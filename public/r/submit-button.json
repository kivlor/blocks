{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "submit-button",
  "title": "Submit Button",
  "description": "A submit button that disables itself and shows a spinner while pending.",
  "dependencies": [
    "lucide-react"
  ],
  "registryDependencies": [
    "button"
  ],
  "files": [
    {
      "path": "components/ui/submit-button.tsx",
      "content": "import type * as React from \"react\";\nimport { Loader2Icon } from \"lucide-react\";\n\nimport { Button } from \"@/components/ui/button\";\n\ntype SubmitButtonProps = React.ComponentProps<typeof Button> & {\n  isPending?: boolean;\n};\n\nexport function SubmitButton({\n  isPending = false,\n  disabled,\n  children,\n  ...props\n}: SubmitButtonProps) {\n  return (\n    <Button aria-busy={isPending} disabled={disabled || isPending} {...props}>\n      {isPending && (\n        <Loader2Icon\n          role=\"status\"\n          aria-label=\"Loading\"\n          className=\"size-4 animate-spin\"\n        />\n      )}\n      {children}\n    </Button>\n  );\n}\n",
      "type": "registry:ui"
    }
  ],
  "type": "registry:ui"
}